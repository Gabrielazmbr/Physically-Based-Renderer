[project]
name = "physically-based-renderer"
version = "0.1.0"
description = "A thesis renderer built as custom Mitsuba 3 plugins"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mitsuba>=3.8.0",
    "pyoidn>=2.5.0.1",
]
